# Google Analytics 4 — Measurement ID (ej: G-XXXXXXXXXX)
# Opcional. Sin esta variable, el componente Analytics.astro no renderiza nada.
# Solo se carga en `pnpm build` (producción); en `pnpm dev` nunca se envían métricas.
PUBLIC_GA_MEASUREMENT_ID=
